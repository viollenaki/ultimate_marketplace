import 'package:flutter/material.dart';

class AppPalette {
  /// Primary actions, buttons, key accents (charcoal).
  static const Color primary = Color(0xFF3A3A3A);
  /// Darker gray for emphasis (prices, pressed states).
  static const Color primaryVariant = Color(0xFF2A2A2A);
  /// Secondary UI accents (selections, links-style highlights).
  static const Color secondary = Color(0xFF5C5C5C);
  static const Color background = Color(0xFFF5F5F6);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1F1F1F);
  static const Color textSecondary = Color(0xFF6C757D);
  static const Color success = Color(0xFF28A745);
  static const Color error = Color(0xFFDC3545);
  static const Color disabled = Color(0xFFADB5BD);
  /// Accent for primary CTAs on the car home (search filter link, sell FAB).
  static const Color accentGreen = Color(0xFF2E9D5C);
  static const Color vipBadge = Color(0xFFE91E8C);

  const AppPalette._();
}
